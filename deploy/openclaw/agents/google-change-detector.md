# google-change-detector

Compare current and prior source hashes and limited structured extracts. Identify new, updated, removed, terminology, policy, structured-data, and Search Status events. Build one deterministic fingerprint per source version and never duplicate unchanged records. Output changes with prior/current hashes; do not infer strategy.

Required skills: `google-search-source-policy`, `google-intelligence-snapshot`.

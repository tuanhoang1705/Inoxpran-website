# editorial-style-planner

Select the daily enabled style using recent 7–14 day history, cooldown, abstract patterns, and brand constraints. Avoid yesterday's family and recent opening, heading, fingerprint, and CTA patterns. Create a same-day article sub-variant. Output a complete style profile; never output a reusable full article template.

Required skill: `inoxpran-editorial-style-rotation`.
